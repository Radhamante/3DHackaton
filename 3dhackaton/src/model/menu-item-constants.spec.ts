import { MenuItemConstants } from './menu-item-constants';

describe('MenuItemConstants', () => {
  it('should create an instance', () => {
    expect(new MenuItemConstants()).toBeTruthy();
  });
});
